import React from 'react'

type JustInItemProps = {
    category: string
    title: string
}

const JustInItem = ({ category, title }: JustInItemProps) => {
    return (
        <div>JustInItem</div>
    )
}

export default JustInItem